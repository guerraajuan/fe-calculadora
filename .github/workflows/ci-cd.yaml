name: Flujo CI-CD

on: 
  push:
    branches: [master]


jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
      - name: muestra contenido de repositoio
        run: ls -la 
        

